# ==============================
# Georef (ubicaciones Argentina)
# ==============================
georef.api.base-url=https://apis.datos.gob.ar/georef/api

# ==============================
# WordPress REST API
# ==============================
# Raíz del sitio. El client usa ?rest_route=/wp/v2/... (permalinks planos).
wordpress.api.base-url=https://inmobiliariafortese.com.ar
# Credenciales wp-admin para JWT Authentication for WP-API (no Application Password).
wordpress.auth.username=${WORDPRESS_USERNAME:}
wordpress.auth.password=${WORDPRESS_PASSWORD:}
wordpress.template.page-id=1215
wordpress.publish.initial-status=publish
wordpress.listing.casas-page-id=349
wordpress.listing.casas-carousel-id=42cba2f
# Mu-plugin Inmo360: Document::save para regenerar post_content.
wordpress.listing.elementor-rebuild-route=/inmo360/v1/elementor/rebuild

# ==============================
# DB
# ==============================
spring.datasource.url=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.wekvnrjqngbdelejudyx
spring.datasource.password=Avellaneda.245
spring.datasource.driver-class-name=org.postgresql.Driver

# ==============================
# JPA
# ==============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# 🔥 CLAVE
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==============================
# FIX pooler Supabase

# ==============================
spring.jpa.properties.hibernate.statement_cache.size=0
spring.datasource.hikari.data-source-properties.prepareThreshold=0

# ==============================
# Hikari
# ==============================
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# ==============================
# Brevo (API REST)
# ==============================
brevo.api.key=${BREVO_API_KEY}
brevo.api.url=https://api.brevo.com/v3/smtp/email
brevo.sender.email=${BREVO_SENDER_EMAIL}
brevo.sender.name=Inmo360

# ==============================
# App
# ==============================
app.frontend.url=${FRONTEND_URL}